<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;800&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="split-auth">

<div class="split-container">

  <!-- LEFT PANEL -->
<div class="left-panel">

  <div class="curves"></div>

  <div class="left-content">

  <div class="left-logo">
    <a href="https://www.brightpathhorizon.com" target="_blank">
      <img src="/images/bphlogo.png" alt="BrightPath Logo">
    </a>
  </div>

  <h1>Hello<br>BrightPath! 👋</h1>

  <p>
    Manage leads, automate workflows, and boost
    productivity with our powerful CRM solution.
    Track every opportunity, never miss a follow-up,
    and close deals faster — all in one place.
  </p>  

</div>


  <!-- Footer at absolute bottom -->
  <div class="left-footer">
    © <%= new Date().getFullYear() %> BrightPathHorizon Technologies
  </div>

</div>


  <!-- RIGHT PANEL -->
  <div class="right-panel">

    <div class="login-box">

      <h2>BrightPath CRM</h2>
      <h3>Welcome Back!</h3>

      <% if (error && error.length > 0) { %>
        <% error.forEach(msg => { if(msg) { %>
          <div class="login-error"><%= msg %></div>
        <% } }) %>
      <% } %>

      <form method="POST" action="/auth/login">

        <input
          type="email"
          name="email"
          placeholder="Email Address"
          required
        >

        <input
          type="password"
          name="password"
          placeholder="Password"
          required
        >

        <button type="submit" class="login-btn">
          Login Now
        </button>

      </form>

      <p class="forgot-link">
        Forgot password?
        <a href="/auth/forgot">Click here</a>
      </p>

    </div>

    <!-- Contact pinned to bottom -->
  <p style="position: absolute; bottom: 24px; left: 0; right: 0; text-align: center; font-size: 12px; color: #9ca3af; font-family: 'Plus Jakarta Sans', sans-serif;">
    Need help? <a href="mailto:contact@brightpathhorizon.com" style="color: #6b7280; font-weight: 600; text-decoration: underline; text-underline-offset: 3px;">contact@brightpathhorizon.com</a>
  </p>

  </div>

</div>

</body>
</html>